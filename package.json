{
  "name": "mcp-security-compliance",
  "version": "0.1.0",
  "description": "MCP server for security compliance frameworks — ISO 27001, NIST 800-53, MITRE ATT&CK, and ISO 27017 with official cross-framework mappings",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
